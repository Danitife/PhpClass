## PHP Classwork:

 Array & Function ChallengeInstructionsCreate a single PHP file named array_challenge.php. 
 Write a function that processes data using the array methods and string concatenation we learned in class.
 The ScenarioYou are managing a roster for a local tech club.
 # Step 1: Create the DataCopy this initial array into your file:php$members = ["Alice", "Bob", "Charlie", "David"];
Use code with caution.
 # Step 2: Write the FunctionCreate a function named updateRoster that accepts the $members array as an argument. 
 Inside the function, perform these tasks in order:
 Add a member: Add "Eva" to the end of the array.
 Add a VIP member: Add "Frank" to the very beginning of the array.
 Remove a member: Remove the last member from the array.
 Remove another member: Remove the very first member from the array.
 Search for a index: 
 Find the position of "Charlie" in the array using a search method. 
 Use string concatenation to print: "Charlie is at index: [index number]" followed by a <br>.Return the list: 
 Have the function return the final updated array.
 # Step 3: Display the ResultsCall your function and store the result in a new variable called $final_roster.
 Use a for loop to print the remaining members inside <h3> tags, like this: <h3>Tech Club Member: [Name]</h3>.
